# [["." for _ in range(size)] for _ in range(size)]

# tic = time.perf_counter()
# function
# toc = time.perf_counter()
# print(f"Time: {toc - tic:0.4f} seconds")
#
# assert type(blob['x']) == type(blob['y']) == type(blob['size']) == int
#             assert 0 <= blob['x'] <= self.height and 0 <= blob['y'] <= self.width and blob['size'] > 0

# Colors
# from colorama import init, Back, Fore
# init(autoreset=True)
# print(Back.RED + '')
# https://www.geeksforgeeks.org/print-colors-python-terminal/

# x = enumerate([1, 2, 3])
# for i in x:
#     print(i)

# @property
    # def fCost(self):
    #     return self.gCost + self.hCost
    #
    # @fCost.setter
    # def fCost(self, fCost):
    #     self.fCost = fCost

# [[Node((yIdx, xIdx)) if x == ' ' else x for xIdx, x in enumerate(y)] for yIdx, y in enumerate(maze)]
# {x: [list(_g[x])] for x in _g if x in test}